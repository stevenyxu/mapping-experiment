%h1= @experiment.public_name
- if @experiment.complete?
  %p Congratuations! You are done!
  = link_to 'Back to home', root_path
- else
  %p 
    %span Click here to proceed in the experiment:
    = link_to @experiment.first_incomplete_phase.name, @experiment.first_incomplete_phase
