= link_to (sound = @test_round.round_prototype.pair_prototype.left_sound).name, test_round_path(@test_round, :test_round => {:guess_id => sound.id}), :method => :put
= link_to (sound = @test_round.round_prototype.pair_prototype.right_sound).name, test_round_path(@test_round, :test_round => {:guess_id => sound.id}), :method => :put

#container Loading the player...

= audio_tag @test_round.clip.attachment.url

:javascript
  $(function(){
    jwplayer('container').setup({
      flashplayer: '/swf/player.swf',
      file: $('audio').attr('src'),
      height: 24,
      controlbar: 'bottom',
      width: 300
    });
  });

